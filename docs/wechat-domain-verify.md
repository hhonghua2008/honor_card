# 微信业务域名校验文件

微信公众平台 → 开发管理 → 业务域名 → 下载 `MP_verify_xxxx.txt`

**放置位置**：本仓库根目录（与 `index.html` 同级），例如：

```
honor_card/MP_verify_abc123.txt
```

推送后应可通过以下地址访问：

```
https://hhonghua2008.github.io/honor_card/MP_verify_abc123.txt
```

验证通过后，web-view 才能加载 H5 编辑器。
